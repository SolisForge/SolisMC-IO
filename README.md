# Solis development workspace

This repository contains a base workspace to work on Solis core projects.

# Use this workspace

First, clone this repo:

```bash
$ git clone git@github.com:SolisForge/workspace.git
```

Then, initialize the repositories:

```bash
$ .solis/init.py
```